#include <Driver/Texture.h>

namespace hyperspace {
	namespace video {
		void	Texture::LoadAPITexture(unsigned char* buffer) {
		
		}

		void	Texture::LoadAPITextureCompressed(unsigned char* buffer) {
		
		}

		void	Texture::SetTextureParams(unsigned int &params, unsigned int &target) {
		
		}

		void	Texture::GetFormatBpp(unsigned int &props, unsigned int &format, unsigned int &bpp) {
		
		}
	}
}